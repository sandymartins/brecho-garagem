import { FormatoDataPipe } from './formato-data.pipe';

describe('FormatoDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDataPipe();
    expect(pipe).toBeTruthy();
  });
});
